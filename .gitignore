map-reduce-test

# Clasp config with script ID (contains project-specific IDs)
.clasp.json
.clasp.*.json

# Per-environment clasp auth (OAuth tokens - NEVER commit these)
.clasprc.json
.clasprc.*.json

# Local installation config
.mailbot.conf
.mailbot.*.conf

# Per-environment deployment wrappers (local only)
install-work.sh
install-private.sh

# API keys and secrets - NEVER commit these
api-key.txt
*.key
*.pem
.env
.env.*

# Node modules
node_modules/

# npm debug logs
npm-debug.log*

# ESLint cache
.eslintcache

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# Build output (if any)
dist/
build/

# Temporary files
*.tmp
*.temp
*.bak

# Remote debug capture dir (may contain secrets)
.remote-debug/
